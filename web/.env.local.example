# Copy to .env.local and fill in values before running `npm run dev`

# Backend API (no trailing slash). On localhost dev, point at local FastAPI.
# On Vercel production, you can omit this — the client defaults to https://laksh-api.fly.dev
# (see web/lib/api.ts). Set explicitly if you use a staging Fly app.
# Dev: http://localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Optional: public R2 base URL for direct asset links (no signed URL needed)
# Leave empty to use signed URLs exclusively.
NEXT_PUBLIC_R2_PUBLIC_BASE=
