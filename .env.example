# Copy this file to .env and add your API key
# Get one at: https://ai.google.dev/gemini-api/docs/api-key
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: CORS origins (comma-separated). Default includes production + localhost.
# CORS_ORIGINS=https://lakshai-production.up.railway.app,http://localhost:8000

# Optional: logging (default INFO). Use DEBUG to see arc pipeline / verbose pose logs.
# LOG_LEVEL=INFO
