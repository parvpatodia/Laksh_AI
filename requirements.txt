# Web framework
fastapi
uvicorn[standard]
python-multipart

# Environment & config
python-dotenv

# AI / ML
google-genai
chromadb

# TTS (primary: Studio Voices; fallback: gTTS)
google-cloud-texttospeech
gtts

# Physics engine & computer vision (headless for server deployment)
opencv-python-headless
mediapipe>=0.10.30
numpy
pandas
scipy

# NBA data (db_seeder)
nba_api

# Production server (single worker on 1 GB Fly machine)
gunicorn

# Cloudflare R2 / S3-compatible object storage
boto3

# Testing (regression, CI)
pytest>=7.0
